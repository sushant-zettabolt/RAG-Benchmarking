# Local RAG Evaluation Stack — fully containerized.
#   docker compose up -d     brings up the 5 serving containers
#   the `harness` profile holds the ingest/evaluate/report toolbox (run on demand)
name: ${COMPOSE_PROJECT_NAME:-nq-rag-eval}

services:
  # ── LLM inference (chat) ───────────────────────────────────────────────────
  # Models are NOT downloaded — supply your own GGUF via MODELS_DIR (mounted at
  # /models) and point CHAT_MODEL_PATH at it.
  llama-chat:
    # Built from PUBLIC llama.cpp source on first `up` (see docker/llama/Dockerfile).
    # GGML_ZENDNN=OFF -> the baseline (ggml CPU) image. run_ab.sh overrides
    # CHAT_IMAGE to nqrag-llama:zendnn per job to swap backends (image-swap A/B).
    build:
      context: docker/llama
      dockerfile: Dockerfile
      args:
        GGML_ZENDNN: "OFF"
        LLAMA_CPP_REPO: ${LLAMA_CPP_REPO:-https://github.com/ggml-org/llama.cpp}
        LLAMA_CPP_REF: ${LLAMA_CPP_REF:-}
    image: ${CHAT_IMAGE:-nqrag-llama:baseline}
    container_name: nqrag-llama-chat
    user: "${HOST_UID:-1002}:${HOST_GID:-1002}"
    # CPU pinning via Docker cpuset (cgroup). Chat gets socket 1 exclusively;
    # embed runs on socket 0 to avoid L3 cache pollution on the chat socket.
    # SYS_NICE is kept for OMP thread priority.
    cpuset: ${CHAT_CPUSET:-}
    cap_add:
      - SYS_NICE
    environment:
      CHAT_MODEL_PATH: ${CHAT_MODEL_PATH:-}
      CHAT_CTX: ${CHAT_CTX:-8192}
      CHAT_BATCH: ${CHAT_BATCH:-512}
      CHAT_UBATCH: ${CHAT_UBATCH:-512}
      CHAT_PARALLEL: ${CHAT_PARALLEL:-1}
      CHAT_NGL: ${CHAT_NGL:-0}
      CHAT_THREADS: ${CHAT_THREADS:-}
      CHAT_EXTRA_FLAGS: ${CHAT_EXTRA_FLAGS:-}
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        if [ -z "$$CHAT_MODEL_PATH" ]; then echo "ERROR: CHAT_MODEL_PATH is not set." >&2; exit 1; fi
        if [ ! -f "$$CHAT_MODEL_PATH" ]; then echo "ERROR: chat model not found at $$CHAT_MODEL_PATH." >&2; exit 1; fi
        export OMP_PROC_BIND=close
        export OMP_PLACES=cores
        ARGS="--host 0.0.0.0 --port 8080 --ctx-size $$CHAT_CTX --n-gpu-layers $$CHAT_NGL --parallel $$CHAT_PARALLEL --no-mmap -fa on --metrics --alias chat-model --model $$CHAT_MODEL_PATH"
        if [ -n "$$CHAT_THREADS" ]; then ARGS="$$ARGS --threads $$CHAT_THREADS"; fi
        ARGS="$$ARGS $$CHAT_EXTRA_FLAGS"
        echo "[llama-chat] /app/llama-server $$ARGS"
        exec /app/llama-server $$ARGS
    volumes:
      - ${MODELS_DIR:-./models}:/models:ro
    ports:
      - "${CHAT_PORT:-8081}:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 30s
    restart: unless-stopped

  # ── Embedding server ───────────────────────────────────────────────────────
  llama-embed:
    # Same build-from-source image as llama-chat (identical build = cache hit, no
    # second compile). Embed always runs the baseline image — the A/B swaps only
    # the chat backend (run_ab.sh), so EMBED_IMAGE stays at baseline.
    build:
      context: docker/llama
      dockerfile: Dockerfile
      args:
        GGML_ZENDNN: "OFF"
        LLAMA_CPP_REPO: ${LLAMA_CPP_REPO:-https://github.com/ggml-org/llama.cpp}
        LLAMA_CPP_REF: ${LLAMA_CPP_REF:-}
    image: ${EMBED_IMAGE:-nqrag-llama:baseline}
    container_name: nqrag-llama-embed
    user: "${HOST_UID:-1002}:${HOST_GID:-1002}"
    # Runs on socket 0 (shares with litellm) to keep socket 1 clean for chat.
    cpuset: ${EMBED_CPUSET:-}
    cap_add:
      - SYS_NICE
    environment:
      EMBED_MODEL_PATH: ${EMBED_MODEL_PATH:-}
      EMBED_CTX: ${EMBED_CTX:-2048}
      EMBED_BATCH: ${EMBED_BATCH:-2048}
      EMBED_UBATCH: ${EMBED_UBATCH:-2048}
      EMBED_NGL: ${EMBED_NGL:-0}
      EMBED_THREADS: ${EMBED_THREADS:-}
      EMBED_EXTRA_FLAGS: ${EMBED_EXTRA_FLAGS:-}
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        if [ -z "$$EMBED_MODEL_PATH" ]; then echo "ERROR: EMBED_MODEL_PATH is not set." >&2; exit 1; fi
        if [ ! -f "$$EMBED_MODEL_PATH" ]; then echo "ERROR: embedding model not found at $$EMBED_MODEL_PATH." >&2; exit 1; fi
        ARGS="--host 0.0.0.0 --port 8080 --embedding --ctx-size $$EMBED_CTX --batch-size $$EMBED_BATCH --ubatch-size $$EMBED_UBATCH --n-gpu-layers $$EMBED_NGL --metrics --alias embed-model --model $$EMBED_MODEL_PATH"
        if [ -n "$$EMBED_THREADS" ]; then ARGS="$$ARGS --threads $$EMBED_THREADS"; fi
        ARGS="$$ARGS $$EMBED_EXTRA_FLAGS"
        echo "[llama-embed] /app/llama-server $$ARGS"
        exec /app/llama-server $$ARGS
    volumes:
      - ${MODELS_DIR:-./models}:/models:ro
    ports:
      - "${EMBED_PORT:-8082}:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 30s
    restart: unless-stopped

  # ── LiteLLM proxy (unifies endpoints; exposes token-usage + TTFT metrics) ────
  litellm:
    image: ${LITELLM_IMAGE:-ghcr.io/berriai/litellm:main-stable}
    container_name: nqrag-litellm
    cpuset: ${LITELLM_CPUSET:-}
    depends_on:
      llama-chat: {condition: service_healthy}
      llama-embed: {condition: service_healthy}
    environment:
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY:-sk-bench-master}
    volumes:
      - ./conf/litellm.yaml:/app/config.yaml:ro
    command: ["--config", "/app/config.yaml", "--port", "4000", "--host", "0.0.0.0"]
    ports:
      - "${LITELLM_PORT:-4000}:4000"
    healthcheck:
      # litellm image has no curl; use its bundled python
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:4000/health/liveliness',timeout=3).status==200 else 1)\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s
    restart: unless-stopped

  # ── Pushgateway (receives ab_backend_active metric from run_ab.sh) ───────────
  pushgateway:
    image: prom/pushgateway:latest
    container_name: nqrag-pushgateway
    cpuset: ${PROM_CPUSET:-}
    ports:
      - "${PUSHGW_PORT:-9091}:9091"
    restart: unless-stopped

  # ── Prometheus (scrapes LiteLLM + both llama servers) ────────────────────────
  prometheus:
    image: ${PROM_IMAGE:-prom/prometheus:latest}
    container_name: nqrag-prometheus
    cpuset: ${PROM_CPUSET:-}
    depends_on:
      pushgateway: {condition: service_started}
    volumes:
      - ./conf/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prom-data:/prometheus
    ports:
      - "${PROM_PORT:-9090}:9090"
    restart: unless-stopped

  # ── Grafana (visualises the Prometheus metrics — live RAG pipeline flow) ─────
  grafana:
    image: ${GRAFANA_IMAGE:-grafana/grafana:latest}
    container_name: nqrag-grafana
    cpuset: ${GRAFANA_CPUSET:-}
    depends_on:
      prometheus: {condition: service_started}
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      # anonymous view so the link opens straight to the dashboard, no login
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Admin
      GF_USERS_DEFAULT_THEME: light
      GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH: /var/lib/grafana/dashboards/rag-flow.json
    volumes:
      - ./conf/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./conf/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    restart: unless-stopped

  # ── AnythingLLM (the RAG application) ────────────────────────────────────────
  anythingllm:
    image: ${ALLM_IMAGE:-mintplexlabs/anythingllm:latest}
    container_name: nqrag-anythingllm
    cpuset: ${ALLM_CPUSET:-}
    depends_on:
      litellm: {condition: service_started}
    cap_add:
      - SYS_ADMIN
    environment:
      SERVER_PORT: 3001
      STORAGE_DIR: /app/server/storage
      VECTOR_DB: lancedb
      DISABLE_TELEMETRY: "true"
      # LLM via LiteLLM (Generic OpenAI provider)
      LLM_PROVIDER: generic-openai
      GENERIC_OPEN_AI_BASE_PATH: http://litellm:4000/v1
      GENERIC_OPEN_AI_API_KEY: ${LITELLM_MASTER_KEY:-sk-bench-master}
      # Which LiteLLM model AnythingLLM generates with. run_ab.sh swaps this to
      # `chat-model-bench` (ignore_eos + fixed n_predict) for fixed-decode A/B
      # runs so both backends emit an identical number of decode tokens.
      GENERIC_OPEN_AI_MODEL_PREF: ${CHAT_MODEL_NAME:-chat-model}
      GENERIC_OPEN_AI_MODEL_TOKEN_LIMIT: ${CHAT_CTX:-8192}
      # Embedder via LiteLLM (Generic OpenAI embedder)
      EMBEDDING_ENGINE: generic-openai
      EMBEDDING_BASE_PATH: http://litellm:4000/v1
      GENERIC_OPEN_AI_EMBEDDING_API_KEY: ${LITELLM_MASTER_KEY:-sk-bench-master}
      EMBEDDING_MODEL_PREF: embed-model
      # BINDING cap (in CHARACTERS — AnythingLLM's splitter counts chars, not
      # tokens) on the text-splitter chunk size. The actual chunk size/overlap is
      # written to the DB by scripts/seed_anythingllm.py (EMBED_CHUNK_WORDS x ~6
      # chars). Keep this >= that char value so the chunk size isn't capped.
      # Default 8192 chars ~= EMBED_CTX(2048 tok) x 4 = the embedder's char capacity.
      EMBEDDING_MODEL_MAX_CHUNK_LENGTH: ${EMBED_MAX_CHUNK_CHARS:-8192}
    volumes:
      - anythingllm-storage:/app/server/storage
    ports:
      - "${ALLM_PORT:-3001}:3001"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:3001/api/ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s
    restart: unless-stopped

  # ── Seed (one-shot) — makes `docker compose up` self-configure AnythingLLM ───
  # Runs once after anythingllm is healthy: registers the API key + provider /
  # chunking settings straight into the mounted AnythingLLM DB, then exits. This
  # is what removes the need to run ./setup.sh — a bare `docker compose up -d`
  # brings up the stack AND seeds it. Idempotent (safe to re-run on every up).
  seed:
    image: nqrag-harness:local
    build: ./harness
    container_name: nqrag-seed
    depends_on:
      anythingllm: {condition: service_healthy}
    environment:
      ALLM_DB: /allm-storage/anythingllm.db
      ALLM_KEY: ${ALLM_KEY:-}
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY:-sk-bench-master}
      CHAT_CTX: ${CHAT_CTX:-8192}
      EMBED_CHUNK_WORDS: ${EMBED_CHUNK_WORDS:-512}
      EMBED_CHUNK_OVERLAP_WORDS: ${EMBED_CHUNK_OVERLAP_WORDS:-100}
    volumes:
      - anythingllm-storage:/allm-storage   # same named volume the app uses
      - ./scripts:/scripts:ro
    restart: "no"
    command: ["python3", "/scripts/seed_anythingllm.py"]

  # ── Harness toolbox (ingest / evaluate / report) — run on demand ─────────────
  # Not started by `docker compose up` (profile gated). Invoke with:
  #   docker compose run --rm harness python ingest.py
  harness:
    build: ./harness
    image: nqrag-harness:local
    container_name: nqrag-harness
    user: "${HOST_UID:-1002}:${HOST_GID:-1002}"
    cpuset: ${HARNESS_CPUSET:-}
    profiles: ["tools"]
    environment:
      ALLM_URL: http://anythingllm:3001
      LITELLM_URL: http://litellm:4000
      PROM_URL: http://prometheus:9090
      CHAT_METRICS_URL: http://llama-chat:8080/metrics
      EMBED_METRICS_URL: http://llama-embed:8080/metrics
      ALLM_KEY: ${ALLM_KEY:-}
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY:-sk-bench-master}
      SLUG: ${SLUG:-nq-bench}
      EVAL_DATASET: ${EVAL_DATASET:-nq_open}
      CORPUS_DATASET: ${CORPUS_DATASET:-BeIR/nq}
      DOC_N: ${DOC_N:-100}
      EVAL_N: ${EVAL_N:-100}
      CORPUS_SCAN: ${CORPUS_SCAN:-20000}
      QUERY_MODE: ${QUERY_MODE:-query}
      JUDGE_MODEL: ${JUDGE_MODEL:-chat-model}
      JUDGE_THRESHOLD: ${JUDGE_THRESHOLD:-0.5}
      JUDGE_BASE_URL: ${JUDGE_BASE_URL:-}
      JUDGE_API_KEY: ${JUDGE_API_KEY:-}
      JUDGE_SUBSCRIPTION_KEY: ${JUDGE_SUBSCRIPTION_KEY:-}
      WARMUP: ${WARMUP:-1}
      EVAL_LIMIT: ${EVAL_LIMIT:-}
      RETRIEVAL_TOPN: ${RETRIEVAL_TOPN:-8}
      RETRIEVAL_SIM_THRESHOLD: ${RETRIEVAL_SIM_THRESHOLD:-0.0}
      DATA_DIR: /data
      HF_TOKEN: ${HF_TOKEN:-}
      HF_HOME: /data/hf-cache
      # ── bulk ingest (write vectors straight into AnythingLLM's LanceDB) ───────
      # BULK_INGEST=1 bypasses the per-doc HTTP upload path: the harness embeds
      # chunks against the embed instance(s) and writes rows directly into the
      # workspace's LanceDB table (mounted via anythingllm-storage). 0 = legacy
      # HTTP API path. run_ingest.sh overrides EMBED_URLS/EMBED_CONCURRENCY per run
      # to fan the embedding out across the data-parallel ingest instances.
      BULK_INGEST: ${BULK_INGEST:-1}
      ALLM_STORAGE: /allm-storage
      EMBED_URL: ${EMBED_URL:-http://llama-embed:8080/v1}
      EMBED_MODEL_NAME: ${EMBED_MODEL_NAME:-embed-model}
      EMBED_REQ_BATCH: ${EMBED_REQ_BATCH:-64}
      EMBED_CONCURRENCY: ${EMBED_CONCURRENCY:-8}
      EMBED_CHUNK_WORDS: ${EMBED_CHUNK_WORDS:-512}
      EMBED_CHUNK_OVERLAP_WORDS: ${EMBED_CHUNK_OVERLAP_WORDS:-100}
      EMBED_MAX_CHUNK_CHARS: ${EMBED_MAX_CHUNK_CHARS:-8192}
    volumes:
      - ./src:/app:ro
      - ./data:/data
      # Write access to AnythingLLM's storage so bulk ingest can create the
      # LanceDB table directly (table name = workspace slug).
      - anythingllm-storage:/allm-storage
    working_dir: /app

volumes:
  anythingllm-storage:
  prom-data:
  grafana-data:
