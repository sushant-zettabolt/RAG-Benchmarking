> **Sample ZenDNN A/B report** — generated by `./run_ab.sh` on AMD EPYC 9R14 (CPU).
> Backends: `baseline` = llama.cpp built without ZenDNN; `zendnn` = same source
> built with `-DGGML_ZENDNN=ON` + `ZENDNNL_MATMUL_ALGO=1`. Model =
> `Llama-3.1-8B-Instruct-BF16`, 100 docs, 12 NQ questions, judge = same 8B.
> Jobs ran **sequentially**, chat server pinned to one NUMA node
> (`CHAT_CPUSET=96-191`, `CHAT_THREADS=96`).
>
> Note: Docker `cpuset` pins CPUs only (not memory), so absolute decode t/s is
> memory-bandwidth-limited here — but both jobs run under the identical
> condition, so the baseline-vs-zendnn ratio is valid. ZenDNN accelerates
> matmul-bound **prefill (1.63x)**; bandwidth-bound decode is unchanged (1.00x),
> matching the main-branch finding.

# ZenDNN A/B Report — baseline vs zendnn

_Same RAG pipeline (AnythingLLM + Google NQ), identical model and queries; only the llama.cpp chat backend differs. `baseline` vs `zendnn`, run sequentially (no CPU contention)._

## Per-stage mean latency

| Stage | baseline (s) | zendnn (s) | speedup |
|---|---|---|---|
| Query embedding | 0.011 | 0.010 | 1.03x faster |
| Retrieval + overhead | 0.087 | 0.084 | 1.04x faster |
| Prompt processing (prefill) | 4.192 | 2.594 | 1.62x faster |
| Generation (decode) | 4.993 | 5.227 | 0.96x faster |
| LLM total (prefill+decode) | 9.185 | 7.820 | 1.17x faster |
| Time to first token | 4.289 | 2.688 | 1.60x faster |
| End-to-end (total) | 9.265 | 7.900 | 1.17x faster |

## Inference throughput (tokens/sec, mean)

| Metric | baseline | zendnn | speedup |
|---|---|---|---|
| Prefill (prompt) t/s | 363.5 | 592.1 | 1.63x |
| Decode (generation) t/s | 5.8 | 5.8 | 1.00x |

## Token sizes & quality (sanity — should be ~equal across jobs)

| Metric | baseline | zendnn |
|---|---|---|
| Prompt tokens (mean) | 1523.7 | 1528.7 |
| Completion tokens (mean) | 28.8 | 30.1 |
| Match rate (LLM judge) | 66.7% | 66.7% |
| Mean judge score | 0.650 | 0.650 |
| Queries ok | 12/12 | 12/12 |

## Summary

- Prefill throughput: **1.63x**  (363.5 → 592.1 t/s)
- Decode throughput:  **1.00x**  (5.8 → 5.8 t/s)
- LLM inference latency: **1.17x faster**
- End-to-end latency: **1.17x faster**  (9.27s → 7.90s)

_Embedding (0.011s / 0.010s) and retrieval should be ~equal across jobs — large drift signals CPU contention or contamination. ZenDNN accelerates matmul-bound prefill more than bandwidth-bound decode, as expected._

---
_Generated by report_ab.py._